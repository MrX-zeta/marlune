<?xml version="1.0" encoding="utf-8"?>
<!-- Metadatos del widget de Marlune. updatePeriodMillis=0: NO usamos el refresco periódico del sistema
     (mínimo 30 min); la sincronización la da la composición viva de Glance observando el StateFlow.
     Redimensionable en horizontal y vertical: la composición elige el layout con LocalSize. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="60dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="60dp"
    android:maxResizeWidth="360dp"
    android:maxResizeHeight="320dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen"
    android:previewImage="@drawable/widget_preview"
    android:description="@string/widget_description"
    android:initialLayout="@layout/glance_default_loading_layout" />
